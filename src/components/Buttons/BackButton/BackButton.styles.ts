import { Button, styled } from "@mui/material";
import { BackArrowDark } from "../../icons";

export const BackButtonStyled = styled(Button)`
  background: none;
  border: unset;
  padding: 0;
  height: fit-content;
`;

export const BackArrowDarkStyled = styled(BackArrowDark)`
  font-size: 10px !important;
`;
